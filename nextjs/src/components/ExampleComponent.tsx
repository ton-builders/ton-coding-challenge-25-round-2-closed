import React from 'react';

const ExampleComponent: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Mini App!</h1>
            <p>This is an example component that can be used within the application.</p>
        </div>
    );
};

export default ExampleComponent;